{
  "name": "vpn-selection",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight, accessible Chinese VPN discovery page.",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "node scripts/server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  }
}
